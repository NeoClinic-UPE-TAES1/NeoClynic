name: Node.js Integration Test

on:
  push:
    branches: [ "development" ]
  pull_request:
    branches: [ "development" ]

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      LOCAL_POSTGRES_USER: ${{ secrets.LOCAL_POSTGRES_USER }}
      LOCAL_POSTGRES_PASSWORD: ${{ secrets.LOCAL_POSTGRES_PASSWORD }}
      LOCAL_POSTGRES_DB: ${{ secrets.LOCAL_POSTGRES_DB }}
      DATABASE_URL: ${{ secrets.LOCAL_DATABASE_URL }}
      JWT_SECRET: ${{ secrets.JWT_SECRET }}

    strategy:
      matrix:
        node-version: [22.x]

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json

      - name: Install dependencies
        working-directory: backend
        run: npm ci

      - name: Start database with Docker Compose
        working-directory: backend
        run: |
          docker compose -f ../docker-compose-dev.yml up -d db

      - name: Generate Prisma client
        working-directory: backend
        run: npx prisma generate

      - name: Run Prisma migrations
        working-directory: backend
        run: npx prisma migrate deploy --name init --skip-seed

      - name: Run Prisma seed
        working-directory: backend
        run: npx prisma db seed

      - name: Build project backend
        working-directory: backend
        run: npm run build --if-present

      - name: Run integration tests
        working-directory: backend
        run: npm run test:integration
